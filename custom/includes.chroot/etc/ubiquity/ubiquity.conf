[d-i]
# ─── Blinbuntu Ubiquity Installer Configuration ───────────────────────────────
# Configuration for the Ubiquity (Ubuntu) installer.
# ───────────────────────────────────────────────────────────────────────────────

# Ubiquity automatically detects and uses MATE desktop installer
# No special configuration needed for basic functionality.

# The installer will show optional packages via the blinbuntu-select-packages
# script that runs after installation completes.
